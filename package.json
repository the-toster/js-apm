{
  "name": "js-apm",
  "version": "0.0.1",
  "description": "Web app performance sampler",
  "main": "dist/Apm.js",
  "types": "dist/Apm.d.js",
  "scripts": {
    "build": "npm run lint && npm run build-ts && parcel build compiled\\Apm.js --no-source-maps ",
    "lint": "eslint . --ext .ts",
    "build-ts": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "the-toster",
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^3.6.1",
    "@typescript-eslint/parser": "^3.6.1",
    "eslint": "^7.5.0",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "network-information-types": "^0.1.0"
  }
}
